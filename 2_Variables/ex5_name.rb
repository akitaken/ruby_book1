=begin

Example one prints 3, this is because the external variable (x) can be
accessed from inside the block.

Example two prints an error because it tries to use a variable (x) that was
created within the block, from outside the block which is not possible.

=end 
